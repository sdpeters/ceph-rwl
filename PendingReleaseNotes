v0.86
-----



v0.87
-----

* Previously, the -f (foreground) or -d (daemonize) flags to a daemon would prevent
  it from writing a pid file, regardless of what the 'pid file' config option is
  set to.  Now the pid file is created regardless of whether those flags are
  specified.
